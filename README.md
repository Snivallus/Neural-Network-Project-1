# Neural-Network-Project-1 MINST Classification

- 创建虚拟环境: `python -m venv venv`
- 激活虚拟环境: `venv/scripts/activate`
- 安装依赖: `pip install -r requirements`
- 详细的代码解释参考报告 `report/project 1.md`
- 在 `code` 目录下运行 `test_train.py` 即可进行测试